<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.database_listview">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".informationclause_popup_avtivity" android:label="개인정보 이용동의"
            android:theme="@style/Theme.AppCompat.Dialog"></activity>
        <activity
            android:name=".serviceclause_popup_activity"
            android:label="서비스 이용약관"
            android:theme="@style/Theme.AppCompat.Dialog" />
        <activity android:name=".clausecheck_activity" />
        <activity android:name=".start_Activity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".member_showing" />

        <service
            android:name=".date_managing"
            android:enabled="true"
            android:exported="true" />

        <activity android:name=".choose_membership" />
        <activity android:name=".MainActivity">

        </activity>
    </application>

</manifest>